Goal Action Planner exploration and prototyping

This is a C# prototype with a Winform wrapper that explores different methods for Goal Action Planning.
The purpose is to select and fine-tune an algorithm for eventual deployment in Unity through a transferrable code base.

Each version of the planner operates on the same set of data.
